module.exports = class SearchPage {
    constructor(id, url, title, pageMatch) {
        this.id = id;
        this.url = url;
        this.title = title;
        this.pageMatch = pageMatch;
    }
};
